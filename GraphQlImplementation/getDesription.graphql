query SearchExample($searchTerm: String!) {
  amazonProductSearchResults(input: {
    searchTerm: $searchTerm
  }) {
    productResults {
      results {
        asin
        title
      }
    }
  }
}
